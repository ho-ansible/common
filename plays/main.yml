#! /usr/bin/env ansible-playbook
# Configure template role
# Replace 'all' with your own inventory group
---
- name: common | misc Linux setup
  hosts: all
  roles:
  - role: common
    tags:
    - common
